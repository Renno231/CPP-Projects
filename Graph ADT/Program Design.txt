Graphs are "free form" which means size is not constant:
use either a vectormap or a series of linked list (like in the hash table assignment)

concepts to impliment:
    loosly coupled 
    "depth first" search
    "breadth first"  search -- apparently use both of these ?

    internal <vector> array to hold the Nodes 
    internal 2d <vector> array to hold the distances/weights between two nodes (e.g., relationships[5][4] or relationships[4][5] returns the distance between node 5 and node 4), if -1 there is no connection

    dijkstras shortest path algo -  choose 2 nodes, A and B, and use recursion to find the shortest available path of the current or already indexed nodes
                                    UNTIL node B is found or until there are no available options

    when addNode is called, the node to add will be tacked onto the end of the internal vector array instead of the ID being passed as the index of the array
    